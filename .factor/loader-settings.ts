import { default as dashboard } from "@factor/dashboard/factor-settings" // 100
import { default as templates } from "@factor/templates/factor-settings" // 100
import { default as ui } from "@factor/ui/factor-settings" // 100
import { default as contactForm } from "@factor/plugin-contact-form/factor-settings" // 100
import { default as blog } from "@factor/plugin-blog/factor-settings" // 100
import { default as tailwind } from "@factor/plugin-tailwind/factor-settings" // 100
import { default as zeno } from "@factor/theme-zeno/src/factor-settings" // 150
import { default as cwd } from "../src/factor-settings" // 1000


export default [ dashboard, templates, ui, contactForm, blog, tailwind, zeno, cwd ]